/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['1000logos.net', 'pokeapi.co', 'raw.githubusercontent.com', 'pngkey.com', 'www.pngkey.com'],
    },
  };

export default nextConfig;
